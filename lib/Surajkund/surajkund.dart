import 'package:flutter/material.dart';

class SurajkundPage extends StatelessWidget {
  const SurajkundPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('SurajkundPage')),
    );
  }
}